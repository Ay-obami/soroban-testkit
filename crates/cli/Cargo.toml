[package]
name = "soroban-testkit-cli"
description = "Coverage, resource-limit discovery, and static audit checks for Soroban contracts."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "soroban-testkit"
path = "src/main.rs"

[dependencies]
clap.workspace = true
